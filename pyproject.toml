[project]
name = "train-eval"
version = "0.1.0"
description = "A full Texel-tuning method pipeline for the Zugblitz chess engine"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.10.8",
    "numpy>=2.4.0",
    "python-chess>=1.999",
    "seaborn>=0.13.2",
    "torch>=2.9.1",
    "tqdm>=4.67.1",
]
